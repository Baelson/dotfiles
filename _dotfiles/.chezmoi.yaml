# Chezmoi configuration (minimal)
# Centralized environment data with optional manual overrides

data:
  # Default environment profile; override via DOTFILES_ENV=work|personal
  env: personal

# Notes:
# - Set DOTFILES_ENV=work to switch profiles during apply/testing.
# - Consider using `.chezmoi.yaml.tmpl` if dynamic templating is needed.
