# External repositories managed by chezmoi
# Using archive method for better control and security
#
# Note: oh-my-zsh, antigen, and dircolors are intentionally NOT managed here.
# They are upstream framework code, not personal configuration.
# Installation is handled by .chezmoiscripts/darwin/run_onchange_after_setup-shell-environment.sh
# so they are installed on new machines but not tracked by chezmoi.

# Note: DuqueClan-Configs is managed separately and not included here
# as it's a private repository with different management requirements
